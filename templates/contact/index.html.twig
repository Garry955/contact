{% extends 'base.html.twig' %}

{% block title %}Contact Form{% endblock %}

{% block body %}
<div class="wrapper">
    <h1>Contact Form</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Submit</button>
    {{ form_end(form) }}

</div>
{% endblock %}
